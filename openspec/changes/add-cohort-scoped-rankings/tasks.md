## 1. packages/db — 品类树扩展 + seed 迁移

- [x] 1.1 `seed.ts` `CATEGORY_NODES`：新增 `乳品 dairy(comparableUnit='per_100ml')` 节点（父=root `beverage`）+ 叶 `牛奶 milk / 酸奶 yogurt / 乳酸菌饮料 lactic-drink`（**三叶 `comparableUnit` 留空/`undefined`、经继承解析得 per_100ml**,与 `soft-drink` 子树同范式,**不**在叶上各绑;ASCII slug 钉死，与 core 枚举/迁移一致）；给**各酒种叶**（`baijiu/wine/spirits/whisky/beer/sake-fruit-wine`，`spirits`≠`whisky` 两叶）**各自**设 `comparableUnit='per_100ml'`（酒种叶是各自的绑定点、无更深后代,故绑在叶;`alcohol` 父保持 `null`）；`computeClosurePairs` 自动覆盖新节点/叶闭包。
- [x] 1.2 新增幂等 DML seed 迁移（`packages/db/drizzle`）：`INSERT OR IGNORE` 乳品节点/叶 + 其 `category_closure` 行；**对已存在的各酒种叶用显式幂等 `UPDATE tag SET comparable_unit='per_100ml' WHERE slug IN ('baijiu','wine','spirits','whisky','beer','sake-fruit-wine')`**（prod 已由 0004 落过这些行=NULL，`INSERT OR IGNORE` 对已存在主键 no-op、翻不动该列，必须 UPDATE）；可重复 apply 无副作用。**`seedTaxonomy()` 也须**对已存在酒种叶行做同一 `comparable_unit` 写入，否则在已 seed 旧库上与迁移产出不等价。**形态钉死**：在既有「两遍 insert(`onConflictDoNothing`)+第二遍 `UPDATE SET parentId`」块**之后**追加**独立** `UPDATE tag SET comparable_unit='per_100ml' WHERE slug IN ('baijiu','wine','spirits','whisky','beer','sake-fruit-wine')`（镜像迁移）；**禁止**把 insert 改成 `onConflictDoUpdate`（会连带覆写 parentId/name）。乳品三叶**不**写 comparable_unit（继承）。
- [x] 1.3 seed/继承单测：乳品全线叶、各酒种叶 `resolveComparableUnit` = `per_100ml`；`alcohol` 父、root = `null`；闭包含乳品/酒种叶到各自祖先。**更新既有「comparable_unit 仅绑软饮一个节点」断言** → P3.5 **自身绑定**（`tag.comparable_unit` 列 IS NOT NULL,**非**解析非空）的 8 个节点（`soft-drink + dairy + 6 酒种叶`；软饮叶/乳品叶列为 NULL、靠继承,故不计入这 8 个）。**新增防漂移用例**：预置旧 P3 树（酒种叶 comparable_unit=NULL）后，分别跑迁移 / `seedTaxonomy()`，断言两路均收敛到酒种叶 `per_100ml`（既有「双 fresh 库等价」用例对存量翻转盲、不可单独作数）。

## 2. packages/core — tier1 关键词规则扩展（纯函数 + 脏标题样本单测）

- [x] 2.1 **先扩 core 叶枚举**：`CategoryLeafSlugSchema` + `LEAF_RULES` 加 9 个新叶 slug（6 酒种 `baijiu/wine/spirits/whisky/beer/sake-fruit-wine` + 3 乳品 `milk/yogurt/lactic-drink`）——这是 core schema 改动（非 calculator）；须与 1.1 的 `CATEGORY_NODES` seed 同变更落地（`seed.ts` 编译期守卫遍历枚举断言每叶已 seed，否则模块加载即抛）。**再扩 tier1 规则**（只产叶、同输入同输出、无 IO，权威关键词/禁用裸字列表见 category-tagging spec）：**乳品**（牛奶/鲜牛奶/纯牛奶/灭菌乳/巴氏→`milk`；酸奶/酸牛奶→`yogurt`；乳酸菌/活菌型→`lactic-drink`；**排除** 椰奶/燕麦奶/植物奶/豆浆/坚果乳；禁裸 `奶`）；**酒类 6 叶各自映射**（`beer`=啤酒/精酿/IPA/拉格，禁裸 `啤`；`wine`=葡萄酒/红酒/干红/干白/赤霞珠/西拉/黑皮诺/长相思/起泡酒/香槟酒，禁裸 `香槟/庄园/BIN`；`baijiu`=白酒/茅台/五粮液/泸州老窖/国窖/洋河/汾酒/酱香型白酒/浓香型白酒，禁裸 `度/浓香/酱香`；`sake-fruit-wine`=清酒/大吟酿/纯米/獭祭/山田锦/果酒/梅酒；`spirits`=洋酒/白兰地/干邑/伏特加/金酒/朗姆/龙舌兰/轩尼诗/人头马；`whisky`=威士忌/whisky/麦卡伦/单一麦芽/苏格兰威士忌/波本——**`spirits` 与 `whisky` 必须分两个 LEAF_RULES、各产各叶,不可合并**）；**漏判软饮**（椰子水/椰汁/椰奶/燕麦奶/豆浆/坚果乳/植物蛋白饮、果汁/橙汁/NFC/西梅汁/桑葚汁/葡萄汁/醋饮/山楂汁[禁裸 `山楂`]、电解质水/泉水/苏打水、浓缩液/黑咖/本草饮/麦冬）；**稀奶油等非饮品**不归任何饮品叶。**仲裁优先级**：软饮叶关键词命中时优先于酒类/乳品（防跨 cohort 误归）——经现有 `LEAF_RULES.priority`（同深度按 priority 排序）实现,**酒类/乳品叶 priority 取 < 10**（低于现有 `drinking-water` 的 10,确保软饮叶恒优先;否则「青岛啤酒风味苏打水」共现样本会破）。
- [x] 2.2 脏标题样本单测：正例（每叶代表样本，取自 prod 实际标题如「MM 全脂纯牛奶」「赤霞珠红葡萄酒」「茅台王子酒 53%vol」「一番榨啤酒」「纯米大吟酿」「麦卡伦单一麦芽威士忌」→`whisky`、「轩尼诗 VSOP 干邑」→`spirits`、「燕麦奶」「椰子水」「NFC 橙汁」「电解质水」）；**反例（跨 cohort 误归,必含）**：`零度可乐`/`可口可乐 0 度`→`carbonated`（**非** baijiu,验仲裁优先+禁裸 `度`）；`啤梨汁`→**待人工/manual**（禁裸 `啤` 故非 beer,且无 juice 全词关键词命中、本期不设「梨汁」关键词=召回缺口非误归）；`香槟色气泡水`→`drinking-water`（命中 `气泡水`,禁裸 `香槟` 故非 wine）；`巴黎水葡萄汁味气泡水`→软饮（命中全词 `葡萄汁`,非 wine）；`山楂酒`→**待人工/manual**（`山楂酒`⊉`山楂汁` 且无酒类关键词,确定性 manual、非软饮）；椰奶/燕麦奶/豆浆→植物饮非乳品；果酒/梅酒→`sake-fruit-wine` 非果汁；稀奶油→不可比/不入饮品叶；**已列入品牌词命中正例**「国窖1573」→`baijiu`、「轩尼诗 VSOP 干邑」→`spirits`（验品牌词确实命中、非长尾）；**未列入纯品牌长尾**「剑南春水晶剑」「水井坊井台」（无类型词、品牌不在规则表）→待人工不强归;**软饮词与酒词共现**样本(如「青岛啤酒风味苏打水」→`drinking-water`,锁定 `LEAF_RULES.priority` 软饮优先序、非仅靠禁裸字)。

## 3. packages/db — repository 读查询适配

- [x] 3.1 确认 `buildRankingsQuery`/`listRankings`/`listCategoryTree` 机制**无需改谓词**：`rankable` 经 `comparable_unit` 自动涵盖乳品/酒种叶；`listCategoryTree` 的 `rankable=comparableUnit!==null` 自动随新绑定收敛；`rankableCount=COUNT(DISTINCT product.id)` 经闭包后代,酒类父自动 `>0`。**cohort 守卫不复用 repository 运行期 `resolveComparableUnit`**（它 round-trip `tag` 表,对未 seed 的合法 cohort slug 解析 null→会与「合法但未 seed→200 []」冲突）；改在 `apps/api` 用编译期静态解析器（见 4.1）。运行期 `resolveComparableUnit` 仍服务打标签算 rankable、不变。
- [x] 3.2 repository 单测更新：酒种叶/乳品叶商品 `rankable=true` 且各自 cohort 榜命中（啤酒榜只含啤酒）；`resolveComparableUnit(酒类父)=null`、`resolveComparableUnit(root)=null`、`resolveComparableUnit(beer/乳品叶)=per_100ml`；`listCategoryTree` 中酒种叶/乳品叶 `rankable=true`、酒类父/root `rankable=false`、**酒类父 `rankableCount>0`**（不再为 0）、可点进节点 `rankableCount==节点榜基数`。
  - **存量被绑定翻转反转的既有断言（必须改,否则跑现有套件即红;按用例名定位、勿依赖行号）**：`taxonomy.test.ts` 用例 `'seeds soft-drink leaves inheriting per_100ml and alcohol null'` 内的 `resolveComparableUnit('baijiu')/('wine') .toBeNull()` 现解析 `per_100ml` → 改为只剩 `beverage`/`alcohol` 父为 null 并重命名该用例；用例 `'alcohol leaf resolves null unit → rankable false even when classified'` 整条与 P3.5 矛盾 → 删除/重写为「酒种叶已分类 → `rankable=true`」（对齐 category-tagging spec「软饮/乳品/酒种叶且单位可算则 rankable」）；用例 `'binds comparable_unit on exactly one node (软饮)'`（断言仅软饮列非空）→ 改为 8 个自身绑定节点。
  - **同被翻转的其它 repo 测试文件（必须一并改,proposal「必改否则现有套件红」承诺涵盖之）**：`category-tree.test.ts` 用例 `'resolves comparableUnit by inheritance and derives rankable = (unit !== null)'`——其断言 `['beverage','alcohol','baijiu','wine','beer','whisky']` 全 `comparableUnit=null ∧ rankable=false` 现对 6 酒种叶反转 → 改为酒种叶 `per_100ml`/`rankable=true`、只剩 `beverage`/`alcohol` 父 null；同文件 `'rankableCount counts closure descendants…'` 断言 `alcohol rankableCount=0` → 改 `>0`（顶部注释「alcohol=0」一并改）。`rankings.test.ts` 用例 `'excludes rankable=false rows (alcohol leaf with per100ml is gated out)'`——其用 `wine` 作 `rankable=false` 例（`seedMember(... leaf:'wine', rankable:false)`）+ 断言 `listRankings('alcohol')===[]` 的前提（酒类叶不可排名）在 P3.5 反转 → 改用「真待人工/无叶」行测 rankable 门,`alcohol` 的空榜语义移交 API 层 cohort 守卫 `400`（repository `listRankings('alcohol')` 仍按闭包+rankable 返回,不再代表「酒类不可排名」）。

## 4. apps/api — /rankings cohort 守卫 + 默认节点

- [x] 4.1 `GET /rankings`：`category` 缺省改 **`soft-drink`**（取代 `beverage`）；校验集仍编译期派生自 `CATEGORY_NODES`（含新乳品/酒种叶）；解析目标节点后加 **cohort 守卫**——用**编译期静态解析器 `resolveComparableUnitStatic(slug)`（纯同步、派生自 `CATEGORY_NODES`、沿 parentSlug 求继承,与 `CATEGORY_SLUGS` 同范式、不查 DB）**：解析为 `null`（root `beverage`、`alcohol` 父）→ `400 invalid-request`（提示跨 cohort、选子分类）。**禁止**用运行期 `repo.resolveComparableUnit`（否则未 seed 的合法 cohort slug 误判 400 + 每请求一次 D1）。其余沿用既有 limit/offset 严格 parse、`persistence-error` 等。
- [x] 4.2 路由单测（apps/api）：
  - `/rankings`：无参 ≡ `soft-drink`（软饮 cohort、非 root 混榜）；`?category=beer`/`wine`/`spirits`/`whisky`/`乳品叶 slug` → `200` 各 cohort（`spirits` 与 `whisky` 各自榜、不跨酒种/不混软饮）；`?category=alcohol`/`?category=beverage` → `400`（cohort 守卫）；未知/非 category/大小写/空串 → `400`；limit clamp/非法/offset 越界沿用。**未 seed 窗口（空 `tag` 表）专测**：`?category=beer`（静态解析 per_100ml）→ `200 []`、`?category=alcohol`（静态解析 null）→ `400`——验守卫走静态 `CATEGORY_NODES`、不因 DB 未 seed 把 `beer` 误判 400。
  - `/categories`：树含乳品子树 + 酒种叶 `rankable=true`/`comparableUnit=per_100ml`、酒类父+root `rankable=false`/`null`；消费契约用 `rankable` 判可点进（酒类父 `rankableCount>0` 但 `rankable=false` 不可点进）；可点进节点 `rankableCount==其 cohort 榜基数`。
  - **P3 既有路由测试翻转清单（按用例名定位、勿依赖行号;必改否则现有套件红）**：`routes.test.ts` 中 `GET /rankings` 默认=root `beverage` 的用例 → 改默认 `soft-drink`；`?category=alcohol` → `200 []` 的用例（如 `'alcohol node (rankable=false subtree) -> 200 + []'`）→ 翻为 `400`；`?category=beverage` 默认放行用例 → 翻为「无参=soft-drink、显式 beverage=400」；`GET /categories` 中断言 `alcohol.rankableCount===0` 及 `beverage.rankableCount = 默认 /rankings 基数`（root 榜）的用例 → 改为 `alcohol.rankableCount>0 ∧ rankable=false`、默认基数对齐 `soft-drink`(root 现 `400`、不再有榜)。**另需逐项翻转(勿漏)**：① 准入 `it.each` 表里 `['root','beverage']→200` 与 `['alcohol …','alcohol']→200` 两行 → 均翻 `400`；② `/categories` 的 alcohol 一致性用例里 `getRankings('?category=alcohol')→[]` 那半个断言 → 翻 `400`（与 `alcoholCount===0→>0` 同一用例的另一半）；③ **`'read failure -> 500 persistence-error'` 用例**：其 `getRankings(app, '?category=beverage')` 在 P3.5 会被 cohort 守卫**先 `400`**（守卫在 repo 之前、静态）→ repo 永不抛、断言 `500` 失败 → 把该用例的 `?category=` 改为**合法 cohort slug**（如 `soft-drink`/`carbonated`）以仍走到 repo throw。**勿误改**：`POST /parse` 响应里 `spec.category==='beverage'`（及 `parser.test.ts` 里的 `spec.category`）是解析器的粗 category hint、**非** /rankings 默认,保持 `beverage` 不动。
  - **`tagging.test.ts` 同被翻转（必改;前提失效）**：用例 `'store-map alcohol leaf → classified-leaf (store-map source), pending NULL, rankable=false'`（标题 `某啤酒礼盒`、native 10012172→beer）整条前提失效——其注释「`啤酒` 不是 tier1 关键词→store-map 唯一信号」+ 断言 `decidedBy:'store-map'` + `rankable=false` 在 P3.5 全反转（`啤酒` 现是 beer tier1 关键词→tier1 直接命中、`decidedBy:'tier1'`；beer 解析 `per_100ml`→`rankable=true`）→ 重写:store-map-only 覆盖改用**不含任何 tier1 酒类关键词的纯品牌标题**（如 `剑南春礼盒`+native→baijiu），并把 store-map 酒种叶的 `rankable` 断言改为 `true`；rankable=false 的覆盖移到真·待人工/coarse 用例。同文件幂等用例里 `某啤酒礼盒 … tier1 miss → 待人工` 的注释/断言同样改（tier1 现命中 beer）。`admin-backfill.test.ts` 仅断言 rankable 跨重跑稳定/字段存在、无酒类具体值,**不**受影响。
  - **`packages/api-client` `categories.test.ts`（fixture 语义陈旧、不红但须刷新）**：该文件手搓 fixture 注释「root…the default ranking」「alcohol node rankable=false, rankableCount=0」语义在 P3.5 过时（root 不再是默认榜、酒类父 `rankableCount>0`）；schema parse 测试仍过（`rankableCount=0` 是合法整数、不报错），但**须刷新** fixture 值/注释对齐 P3.5（root 非默认、`alcohol.rankableCount>0`），避免示例语义误导。
  - **确认不受影响（穷举 grep 结论,勿改）**：`client.test.ts`（仅逐字序列化 URL、不校验 slug 语义）、`category-rules.test.ts`（无叶枚举完整性断言,扩叶纯增量,既有用例标题不含新关键词）、`admin-backfill.test.ts`（仅断 rankable 跨重跑稳定/字段存在）、`schema-boundary.test.ts`（`DISTINCT comparable_unit` 仍 `['per_100ml']`、新绑定同值不引入新 distinct）、`parser.test.ts`（仅用 `spec.category` 粗 hint、酒类标题走数量/计算路径）、`d1-reconcile.test.ts`（喂合成字面 `leafSlug` + 显式 rankable、与关键词规则无关）——均**不**随绑定翻转变红。

## 5. 文档

- [x] 5.1 `docs/taxonomy-and-tagging.md`：树补乳品节点；标注各酒种叶绑 `per_100ml`（部分酒类可做 per100ml cohort 榜）、per100ml 仅同质 cohort 内有效、酒类父/root 不出混榜；§八未决「酒类可比单位」收敛为「叶级 per100ml cohort 榜 + 度数 value 榜留 v2」。`docs/architecture.md`：`/rankings` 默认软饮 + cohort 守卫（跨 cohort 节点 400）。

## 6. 存量纠正 + 验收（合并后 / 运维）

- [ ] 6.1 合并后重跑 `POST /admin/backfill`（幂等）重分类存量（乳品/酒类/漏判软饮按新 tier1 规则 + 新树落叶、重算 rankable）。
- [ ] 6.2 重跑数据就绪门（只读 prod）：各 cohort 榜基数合理（软饮 ~68、牛奶/啤酒/葡萄酒/白酒… 各 cohort 有数）；残留「`per100ml 非空 ∧ rankable=0 ∧ 无 kind=category 叶`」B 仅由**已确认非软饮长尾**（品牌酒标题无关键词、稀奶油等）构成 → 接受（不进榜、不混 cohort）；彻底收靠后续 store-map（非本期）。
- [ ] 6.3 归档：实现合并后经 `/opsx:archive` 把**四份 MODIFIED 增量**（rankings-api / category-tagging / category-tree-api / persistence）并入主 spec（本期无新建/改名需求，category-tagging 标题已是 P3 收敛态、无 4.5 式更名门）。

## 7. 非本变更范围（记录）

- [ ] 7.1 （非目标）per-纯酒精/度数轴（白酒/烈酒更公平的 value 榜，需 `度数` attribute）→ v2。
- [ ] 7.2 （非目标）store-map native-id ingest 接通（收高端酒长尾 + 跨店）→ 后续独立提案。
- [ ] 7.3 （非目标）miniapp 分类树 Tab 接通各 cohort 榜 → 后续小程序变更。
