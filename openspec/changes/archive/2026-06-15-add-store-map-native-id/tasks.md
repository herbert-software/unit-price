## 1. packages/db — product_raw 加列 + 迁移 + upsertRaw/backfill 读写

- [x] 1.1 `schema.ts` `productRaw` 加可空列 `native_category_id`(`text`,可空);经 `drizzle-kit generate` 出 DDL 迁移(ADD COLUMN,登记 `_journal.json`)。**必按此防撞序列**(否则 prod 迁移撞车):generate 产出会是 `0004_<slug>.sql`(journal 末位 idx=3+1),与磁盘既有、不登记 journal 的 `0004_seed_taxonomy.sql`/`0005_*` 撞前缀且在 prod `wrangler d1 migrations apply` 目录扫描乱序——手工改名 `0004_<slug>.sql`→**`0006_<slug>.sql`**、`meta/0004_snapshot.json`→`meta/0006_snapshot.json`、journal 新条目设 **`idx:6, tag:"0006_<slug>"`**(**idx 必须=6**——留 4 则下次 generate 取 `max(idx)+1`=5 撞磁盘 `0005_seed_dairy`;drizzle 以 tag 定位文件、idx 4/5 留空档无碍),再跑一次 `generate` 确认 `git status --porcelain packages/db/drizzle` 干净(过 CI drift 守卫 `ci.yml:41-49`;该守卫只查 generate 幂等、**不查前缀唯一/单调**,故改名+正确 idx 是唯一安全网)。
- [x] 1.2 `repository.ts` `upsertRaw`:写入 `native_category_id`;`onConflictDoUpdate` 对该列走 **COALESCE**(`coalesce(新值, 旧列)`,与 source/sourceUrl 同语义——重报带值则更新、省略则留旧、price-only 重报不清空)。`UpsertRawInput` 加可空 `nativeCategoryId`。
- [x] 1.3 `repository.ts` / backfill 读路径:`listProductsForBackfill`(`apps/api/src/tagging.ts`)select `product_raw.native_category_id` 并放进每条记录(取代硬编码 `nativeCategoryId: null`)。**注**:接通后多数行 native 非空,`tagProduct` 每条会多一次 `lookupStoreCategory` D1 子请求——`ADMIN_BACKFILL_MAX_LIMIT` 的子请求预算注释("limit×9≤50")是按 LAZY(无 lookup)路径写的,接通后须按「每商品 +1 lookup」重核(延续既有「勿在实测前把 5 当 load-bearing」纪律)。
- [x] 1.4 单测:upsertRaw 写 + COALESCE(带值→更新、省略→留旧、price-only 重报不清空);加列迁移幂等可 apply(对非空 `product_raw` 加可空 TEXT、既有行该列=NULL,参照 `taxonomy.test.ts:495` 非空表 ALTER 模式);listProductsForBackfill 返回 native_category_id。**端到端 backfill-read 测试**:落一行带 `native_category_id` 的 `product_raw`(经 upsertRaw 新字段、**非** category_hint)→ `listProductsForBackfill` 读出该列 → `runBackfill` 触发 store-map 命中。**改造既有 `tagging.test.ts` `landProduct` 助手**:把 nativeCategoryId 经 `upsertRaw` 新字段落库(去掉现把它塞进 `category_hint` 的 scaffolding,`tagging.test.ts:71/77`),并修陈旧注释(去 "rides on category_hint"/"store-map lazy this period"/不存在的 "design D9" 引用)。**并改写既有 backfill 测试 `tagging.test.ts:414-506`(原断言「store-map LAZY in backfill」)**:① 「backfills every product via tier1」用例的计数断言(classified/pending/manual,~:453-455)须按 store-map 接通后重算(helper 落库的 native-id 现经 store-map 点火:命中叶→classified、命中粗节点→pending);② 「backfill is store-map LAZY: native-only product lands 待人工」用例(~:476)前提反转——native-only 商品现经 store-map 定叶→classified,须改写为反向断言(或删除并以本任务的端到端 backfill-read 测试替代)。否则实现 1.3/1.4 后这两测试必红、CI build 失败。

- [x] 1.5 `tagging.ts` `BackfillResult` 加 `storeMapDecisions: number`(runBackfill 累加 `result.verdict.verdict==='leaf' && result.verdict.decidedBy==='store-map'` 的条数——即 store-map **定叶**的决定数;按设计**不含**同叶认同[记 tier1]与粗 native[落 pending]);`routes.ts` `/admin/backfill` 响应投影该字段;`docs/backfill-runbook.md` 响应字段表补 `storeMapDecisions` + 注明 **backfill 分块续跑,门值须跨所有块累加**(单块响应非全量)。使 6.3「store-map 决定数 > 0」门有部署面可读出(否则 BackfillResult 无该计数、恒不可测)。单测:store-map 定叶样本 → storeMapDecisions>0;同叶认同样本不计入。

## 2. apps/api + packages/api-client — ingest/contribute 采集 native-id

- [x] 2.1 `routes.ts` `ContributeRequestSchema`(及 `/ingest`、`/ingest/batch` 共用 envelope、单一落库映射 `upsertRawOrNull`)加**可空专用字段** `nativeCategoryId`(provenance 层,**不**进 core `RawProductSchema`、**不**复用 `categoryHint`);在单一落库映射 `upsertRawOrNull`(`routes.ts:394-401`)的 `repo.upsertRaw({...})` 加 `nativeCategoryId: req.nativeCategoryId`(三端点共用此映射,改一处即覆盖 /ingest、/ingest/batch、/contribute)。**空串/纯空白须落 null 而非 400**(契约见 contribute-ingest spec):用 `z.preprocess((v)=> (v == null || (typeof v==='string' && v.trim()==='') ? undefined : v), z.string().trim().min(1).optional())`(或等价 transform)——`min(1)` 只校验**有意义的非空值**,**显式 JSON `null` / 空串 / 纯空白同样归为「省略」→ null+成功**(合「可空」契约,兼容生成式客户端把缺省序列化为 null)。**勿**用裸 `z.string().trim().min(1).optional()`(它把空串/`null` 判 400,与 spec「空/null→null→成功」冲突)。
- [x] 2.2 `packages/api-client`:**无改动**——经核 api-client 只含读路径契约(rankings/categories),无 ingest/contribute 请求 schema;ingest body 校验唯一落点是 `routes.ts` `ContributeRequestSchema`(2.1 已覆盖)。本条仅记录"无需改"以免实现者空找。
- [x] 2.3 路由单测:带 `nativeCategoryId` → 落 `product_raw.native_category_id`、不改 `categoryHint`/`product.category`;**省略/空串/纯空白/显式 null → 落 null 且 200/202(非 400)**、错误码集不变;非字符串(如数字)→ 400;旧 body 不带它行为不变(非 BREAKING)。

## 3. packages/core — 仲裁优先级反转(native 叶 ≻ tier1 叶)

- [x] 3.1 `category-rules.ts` `arbitrate(tier1, storeMap)`:`tier1 有叶 ∧ storeMap.kind==='leaf'` 时——**异叶**(store-map 叶 ≠ tier1 叶)优先返回 store-map 叶(`decidedBy=store-map`,取代「tier1 叶覆盖」);**同叶**(store-map 叶===tier1 叶)仍取该叶、`decidedBy=tier1`(叶相同不翻 provenance,避免 churn `product_tag.source`)。保留 ① 粗 native(`coarse`)仍 < tier1 叶(tier1 命中则取 tier1 叶、否则 pending 指粗节点)② native miss → tier1 ③ 都 miss → manual。新全序 `native 异叶 ≻ tier1 叶 ≻ 粗 native(pending) ≻ 待人工`(同叶取该叶、记 tier1)。
- [x] 3.2 仲裁单测(taxonomy §五 九格表):**异叶翻转**——把既有 `category-rules.test.ts:315-317`(异叶抽象格 `tier1=leaf(carbonated) ∧ storeMap=leaf(juice-plant)`)改判 → `juice-plant`/`decidedBy=store-map`;**新增** `tier1=leaf(milk) ∧ storeMap=leaf(juice-plant)` → `juice-plant`(纠 `燕麦牛奶` 误判正例);`tier1=miss ∧ storeMap=leaf(baijiu)` → `baijiu`(填 `剑南春` 长尾);**同叶保持**——`tier1=leaf(carbonated) ∧ storeMap=leaf(carbonated)` → `carbonated`/`decidedBy=tier1`(`category-rules.test.ts:320-322` **不变**);`tier1=leaf ∧ storeMap=coarse` → tier1 叶;`tier1=tie ∧ storeMap=coarse` → pending(指粗节点,补此格保九格全覆盖);`storeMap=none` → 全走 tier1(口径同 P3.5)。(测试助手:milk×juice-plant 的 store-map 侧 `juice-plant` 已在 `smLeaf` 联合内、baijiu 用既有内联 `{kind:'leaf',leafSlug:'baijiu'}`,无需改助手。)更新 `docs/taxonomy-and-tagging.md` §五 九格表**异叶**那格(同叶维持 tier1)。

## 4. 运维工具 — HAR 提取器抽 categoryIdList 叶 id

- [x] 4.1 HAR 提取器(eval-harness / 抓包脚本)**新抽**每条商品的 `categoryIdList` **路径末端叶 id** + `(store, storeSku)`,产出**供 native-id-only `UPDATE` 回填的幂等 SQL 文件**(非 /ingest 重放;已用真实 HAR 验证 `categoryIdList` 存在、稳定数值路径、簇纯净,见 [[sam-har-calibration]])。**显式产出 storeSku**(与当初 ingest 落库去重键同源);执行 UPDATE 前先只读 join-rate 校验抽取键与既有 `product_raw` 命中率(见 6.1),勿假设「对齐天然」。**(已执行,一次性抓包脚本:旧 HAR 抽 440 商品,leaf=categoryIdList[-1],storeSku=String(spuId);storeSku=spuId 经 6/6 抽样实证;eval-harness 永久化为后续可选扩展。)**
- [ ] 4.2 (运维)一次**软饮足量**山姆 HAR 抓取,补 `seed.ts` `SAM_CATEGORY_MAP`:`咖啡·茶饮`/`饮用水` 软饮叶 native id(原 HAR 偏酒类缺这两叶)+ 更多酒种叶 id。**新增行须同步新增目录扫描 DML 迁移 `0007_seed_<slug>.sql`**(编号 = 当时磁盘最高前缀 0006+1,遵 D2 不变式;镜像 0004/0005 风格、`INSERT OR IGNORE`、**不登记 journal**、`store_category_map` 行 id 用与 `seedTaxonomy()` 同一确定性 id 方案);**并把它接进 seed-parity 测试的 `apply0004()`**(`taxonomy.test.ts:918`,只 apply 0004+0005 → 加 0007,建议重命名 `applySeedMigrations`),否则即便有 0007 文件,parity(`taxonomy.test.ts:904-1011` 断言 `seedTaxonomy()` ≡ DML byte-identical)仍因测试侧未 apply 0007 而失败。**注意 D2 不变式连锁**:落 `0007_seed` 后磁盘最高前缀变 0007,**再下一个 DDL generate 须改名到 `0008`**。(扩覆盖项,缺它不阻断核心接通,但**一旦动 `SAM_CATEGORY_MAP` 即必带 0007 + apply0004 接线**。)

## 5. 文档

- [x] 5.1 `docs/taxonomy-and-tagging.md`:§五 九格表同粒度**异叶**冲突由「tier1>store-map」改「native 叶≻tier1」(同叶仍记 tier1);§七.1 标注 native-id 已接通(ingest 字段 + product_raw 列 + backfill 读)、HAR 提取器抽叶 id;`docs/backfill-runbook.md` 补「native-id-only UPDATE 回填 + 重跑 backfill + 精度抽样」一节、并**厘清两类回填**(`runBackfill` 重打标签是**重读列再打标签**;native-id 回填是**单独的 native-id-only `UPDATE` 步骤**——两者皆**不重放 /ingest**,勿混)。同时清理 §五 ④ 残留「若有 LLM 候选过 guard 则用」与「LLM 本期不参与」口径不一(改为本期无 LLM 候选);`docs/architecture.md` 数据模型除补 `native_category_id` 外去掉「tag 表由后续变更引入」陈旧表述(taxonomy 已上线);清理 `apps/api/src/tagging.ts` 多处「store-map LAZY this period / backfill passes null / no ingest field carries native-id yet」陈旧注释(`tagging.ts:38-46/192-200/234-236/242-248`——接通后均失真;保留「勿复用 category_hint」规则但理由改为「它是 product.category 透传源」而非「尚无 native 字段」),含校正 `runBackfill` docstring 陈旧的「≈445」为当前 prod ~376。

## 6. 合并后(运维 / 验收)

- [x] 6.1 合并部署后:**先**只读校验 HAR 抽取 `(store,storeSku)` 与既有 `product_raw` 行键 join-rate(命中率过低先查 key 口径,勿盲灌);再 HAR 提取器抽存量 native 叶 id → **产出幂等 SQL 文件**(每行 `UPDATE product_raw SET native_category_id = COALESCE(native_category_id, '<v>') WHERE store='<s>' AND store_sku='<sku>';`——**只补空**:保留已有值、仅填 null) → 经 **`wrangler d1 execute DB --env production --remote --config apps/api/wrangler.toml --file <生成.sql>`**(`--config` 必带,否则从仓根解析不到 `DB` 绑定)对既有 ~376 行执行(**只补 native 列、不碰 title/price、不触发解析、不走 /ingest、不新增 admin 路由**;漏配行留 null 退化 tier1)。`d1 execute` 不被 `check-no-prod-drizzle-migrate.sh` 守卫拦。**(已执行:旧 HAR 440 商品 → native_set 0→435;prod sam 原始行 942[非 376,后者是已解析 product 数]。)**
- [x] 6.2 重跑 `POST /admin/backfill`(幂等,见 backfill-runbook):native 已落行经 store-map 重分类、归属变化重算 rankable。**(已执行:keyset 循环 182 轮、limit=5、浏览器 UA 绕 CF 1010;processed=909。)**
- [x] 6.3 数据门(只读 prod):待人工 ↓(P3.5 残留 ~29 的高端酒长尾被 native 叶兜住);tier1 跨 cohort 误判(燕麦牛奶等)被 native 纠正;**store-map 精度抽样(必做,见 7.4)**:抽样 tier1 叶在 store-map 下被改写的商品,**人工**核对其标题语义 / 山姆自身展示分类与 store-map 落叶是否一致(**离线判定,非只读 prod 终态**——`product_tag.source` 只存终态、反推不出 tier1 本会判什么;识别"被改写"样本需离线重放 `tagTier1Leaf` + `lookupStoreCategory`[后者是 Repository 方法,须对 prod 快照 DB 建 repo]),出现 tier1-对→store-map-错=blocker(回滚该 `SAM_CATEGORY_MAP` 行再宣告);`store-map 决定数 > 0`(读 `/admin/backfill` 响应 `storeMapDecisions`,**跨所有续跑块累加**,见 1.5);各 cohort 基数更准、无跨 cohort 混。**(已执行:storeMapDecisions=29→25;待人工 422→402;rankable=507;精度抽样抓到 4 个山姆交叉上架误路由[法国红葡萄酒/花雕/青梅酒/脱醇葡萄汁]→ 对应商品 native_id 置 NULL 退回 tier1 后重跑修复;~9 个高端白酒[国台/习酒/水井坊/牛栏山/古井贡/西凤/酒鬼]由 store-map 从待人工兜回。)**
- [x] 6.4 归档:经 `/opsx:archive` 把三份 MODIFIED/ADDED 增量(contribute-ingest / persistence / category-tagging)并入主 spec。**(已执行:3 份增量已同步进主 spec、变更目录归档至 archive/2026-06-15-add-store-map-native-id。)**

## 7. 非本变更范围(记录)

- [ ] 7.1 (非目标)跨店同款匹配 / 跨店归一(native-id 接通是其前置,归一留后续)。
- [ ] 7.2 (非目标)per-纯酒精/度数轴 value 榜 → v2。
- [ ] 7.3 (非目标)miniapp 分类树 Tab 接通各 cohort 榜 → 后续小程序变更。
- [x] 7.4 store-map 精度评测(校验 native 优先的净精度升)。**因 D5 反转令 blast radius 增大,其最小版为本期回填验收门(见 6.3)、非可选**。**(已执行最小版:人工抽样 29 个 store-map 决定,抓 4 个交叉上架误路由并修复;25/25 余下决定经核对正确/同 cohort 内。全量自动化评测仍为后续。)**最小版(本期必做,纯人工抽样、无新代码):离线脚本对每条商品跑 `tagTier1Leaf(title)` + `lookupStoreCategory(store, nativeId)`,筛 `tier1.leaf != storeMap.leafSlug ∧ 两者皆非空`(= 被 store-map 改写的 tier1 叶)的样本 → **人工**核对标题语义 / 山姆展示分类与 store-map 落叶是否一致;tier1-对→store-map-错=blocker(回滚该 `SAM_CATEGORY_MAP` 行)。**注**:eval-harness 当前**无** native 叶真值字段(corpus 只有 `samPkgNum`,无 `samCategoryLeafId`);自动化精度评测(给 `packages/eval` corpus 加 native 叶真值字段 + HAR 提取器产出该字段,做全量自动比对)是**后续扩展**,本期门不依赖它。
