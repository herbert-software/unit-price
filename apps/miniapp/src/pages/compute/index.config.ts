export default definePageConfig({
  // Non-tab page reached via navigateTo from 榜单 home (search no-result CTA +
  // compact entry beside the search bar). Has a back button.
  navigationBarTitleText: '算它值不值',
});
