{
  "name": "unit-price",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "engines": {
    "node": ">=22"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "workerd"
    ]
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "lint": "echo \"lint: placeholder (no linter configured yet)\""
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.9",
    "wrangler": "^4.105.0",
    "zod": "^4.4.3"
  }
}
