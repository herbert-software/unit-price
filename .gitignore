node_modules/
dist/
*.tsbuildinfo
.DS_Store
.env
.env.local

# eval harness: raw captures & corpus products are not redistributable.
# Desensitized samples and the derived metrics baseline are kept.
*.har
packages/eval/corpus/*
!*.sample.jsonl
!baseline.json

# local sqlite databases (drizzle-kit migrate targets, test fixtures)
.local/
